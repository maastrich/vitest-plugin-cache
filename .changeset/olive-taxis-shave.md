---
"@maastrich/vitest-plugin-cache": patch
---

Publish a real version range for `@vitest/runner` instead of the pnpm `catalog:` protocol, which shipped verbatim in 0.1.0 and broke installs outside the workspace.
